# Project Title

Blueground assignment


## Docker commands

In the project you will find attched a docker-compose file where the app and a mysql db are located. You will also find an init sql script which contains the database schema and data. 

- docker-compose down -v
- docker-compose build
- docker-compose up -d

